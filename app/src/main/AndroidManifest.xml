<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mysecondapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mysecondapp">
        <activity
            android:name=".ReceiptActivity"
            android:exported="true"
            android:label="@string/receipt_page_title"
            android:parentActivityName=".BookingsActivity" />
        />
        <activity
            android:name=".Aminity"
            android:exported="true"
            android:label="@string/aminity_page_title" />
        <activity
            android:name=".RoomDesc"
            android:exported="true"
            android:label="@string/about_room_page_title"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".BookingsActivity"
            android:exported="true"
            android:label="@string/bookings_page_title" />
        <activity
            android:name=".BottomNavigation"
            android:exported="true" />

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>